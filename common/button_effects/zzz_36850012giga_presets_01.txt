giga_presets_01 = {
    potential = {
        has_country_flag = giga_menu_page_presets
    }
    allow = {
        custom_tooltip = {
            fail_text = "must_not_have_started_game"
            NOT = {
                has_global_flag = giga_game_started
            }
        }
        custom_tooltip = {
            fail_text = "must_have_user_preset_mod"
            NOT = {
                has_giga_preset_mod = yes
            }
        }
    }
    effect = {
        hidden_effect = {
            country_event = {
                id = giga_preset.1
            }
            giga_preset_01 = yes
        }
    }
}
