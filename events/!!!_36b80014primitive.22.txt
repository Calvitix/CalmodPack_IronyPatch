namespace = primitive
country_event = {
    id = primitive.22
    hide_window = yes
    trigger = {
        is_country_type = primitive
        has_country_flag = iron_age
        NOT = {
            has_country_flag = recently_advanced
        }
    }
    mean_time_to_happen = {
        years = 1000
    }
    immediate = {
        set_timed_country_flag = {
            flag = recently_advanced
            days = 360000
        }
        set_primitive_age_effect = {
            primitive_age = late_medieval_age
        }
        change_country_flag = {
            icon = {
                category = "special"
                file = "primitive.dds"
            }
            background = {
                category = "backgrounds"
                file = "new_dawn.dds"
            }
            colors = {
                "light_blue"
                "blue"
                "null"
                "null"
            }
        }
        capital_scope = {
            if = {
                limit = {
                    exists = observation_outpost_owner
                }
                observation_outpost_owner = {
                    country_event = {
                        id = primitive.23
                    }
                }
            }
        }
    }
}
